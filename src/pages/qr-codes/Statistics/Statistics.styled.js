import styled from 'styled-components'

import { Text } from 'styled'

import { TipCard, TipCardTop, TipCardMain, TipCardRow, RatingCell } from 'components/TipsTable/TipsTable.styled'

export { Text, TipCard, TipCardTop, TipCardMain, TipCardRow , RatingCell }